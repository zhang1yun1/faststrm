# 版本更新日志

本页面记录 FastStrm 所有历史版本的更新内容，按「新增 / 优化 / 修复」分类，方便快速浏览。

> 📌 最新版本：**v1.3.5** · [GitHub Releases](https://github.com/wabisabi926/faststrm/releases)
>
> 🎯 v1.3.5 亮点：生活监控不再生成空白的字幕/海报/刮削占位文件，改为真实下载网盘里的对应文件。

***

## v1.3.5 · 2026-09-22 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.3.4...v1.3.5)

生活监控的字幕 / 海报 / 刮削文件更干净了。

- 不再生成空白的 SRT / NFO / ASS / JPG 占位文件，改为按「下载文件扩展名」真实下载网盘里的对应文件；网盘没有的就不再生成，目录更清爽

## v1.3.4 · 2026-09-17 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.3.3...v1.3.4)

原盘播放更省心的一版。

### 亮点速览

- ISO / BDMV 原盘不再占用服务器带宽：改成 302 直连网盘播放，流量直接走网盘，Kodi/next-gen 免转码直接看
- 网络扫描修复：带子目录的资源不再被整批误判为失效

### ⚡ 优化

- ISO / BDMV / M2TS 等原盘格式由「服务器中转」改为「302 直连网盘」：播放器拿到直链后直连网盘拉流，服务器不参与传输，流畅看 4K 原盘也不吃 NAS 带宽

### 🐛 修复

- 网络扫描时按子目录递归比对云端目录，修复带子目录的资源被全部误判为失效的问题

## v1.3.3 · 2026-09-16 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.3.2...v1.3.3)

生活监控路径更准、日志更省心的一版。

### 亮点速览

- 生活监控：一次性上传整个文件夹时，每个文件只生成一次 STRM，不再因重复事件重复生成
- 生活监控：文件类型与文件夹类型分开判断路径，文件不再被错误当成文件夹去查，路径匹配更稳
- 生活监控：文件丢失「父目录」信息但本地已有记录时，可直接用记录找回完整路径，不再退回裸文件名导致跳过
- 日志文件改为自动滚动：单个日志到一定大小自动归档压缩并清理旧档，磁盘不会无限增长

### ⚡ 优化

- 生活监控：路径改为按「文件 / 文件夹」类型区分解析，文件夹递归和文件事件各走各的正确逻辑，减少错配导致的映射落空
- 生活监控：文件缺省父目录信息时，先查本地已有的路径记录（之前处理过就能找回），找不到才退回最后一档

### 🐛 修复

- 生活监控：上传整个文件夹时，同一文件因「文件夹递归」和「文件级事件」各处理一次而重复生成 STRM 的问题，改为互斥去重
- 日志文件无限增大、占用磁盘的问题，改为按大小自动滚动并保留最近若干份、过期自动清理

***

## v1.3.2 · 2026-09-15 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.3.1...v1.3.2)

Emby 反代端口支持播放 ISO 原盘、生活监控更稳、启动更快的一版。

### 亮点速览

- Emby 通过反代端口也能正常播放 ISO 原盘 / BDMV 等原盘格式（之前会转圈打不开，现在走代理流、可正常拖动进度）
- 生活监控整体加固：同一文件重复事件不再重复处理，事件漏拉、文件换位置后 STRM 补不上的问题一并修复
- 启动速度提升：Telegram 机器人和生活监控自动启动不再拖慢 Web 服务打开
- 生活监控移动文件默认使用本地移动（更快），路径映射按钮位置更清晰

### ✨ 新增

- Emby 反向代理：STRM 源文件名以 STRM 文件内容为准解析，ISO / BDMV / M2TS / TS 等原盘与直播格式在反代端口走代理流播放（支持 Range 拖动），普通 MKV / MP4 仍保持高速 302 直连
- 反代端口缓存未命中时自动实时查询 PlaybackInfo，减少「打不开 / 转码失败」的兜底路径

### ⚡ 优化

- 启动速度：Telegram 机器人自动轮询改为异步启动并缩短拨号超时，不再因网络拨号拖住 Web 服务首次打开
- 生活监控：事件游标改为「ID + 时间」成对推进，断点续传更准确，重启后不易丢事件
- 生活监控：事件分页按数量正确终止，账号验证只拉取必要范围，减少无效请求
- 生活监控：移动文件默认模式改为本地移动（`local_move`），移动效率更高
- 生活监控：路径映射的「添加 / 删除」按钮移到本地路径输入框之后，操作更直观

### 🐛 修复

- Emby 反代端口无法播放 ISO 原盘的问题：之前因文件名判断不到扩展名而走错路径，现在可从 STRM 文件正确识别并走代理流
- 浏览器（Emby Web）播放原盘时的跨域问题：反代自动拦截 crossorigin 并修补播放器脚本，避免 CORS 导致黑屏或失败
- 生活监控：同一文件出现重复事件时重复生成 / 重复删除的问题，改为按 file_id 去重
- 生活监控：新文件事件偶发拿不到下载标识导致 STRM 未生成的问题，增加兜底反查

***

## v1.3.1 · 2026-09-05 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.3.0...v1.3.1)

入库通知更快、播放停止通知更简洁、设置保存更安全的一版。

### 亮点速览

- 入库通知速度大幅提升：刮削等待从最慢 43s 降到 25s，海报卡住不再拖 5 分钟
- 播放停止通知移除冗余的「观看时长」，播放进度已有时间不再重复
- Emby 设置保存时不再把脱敏 API Key 写回配置，避免真实 Key 被覆盖
- 新增 `--no-tray` 启动参数，无桌面环境也能跑

### ✨ 新增

- 新增 `--no-tray` 启动参数，跳过系统托盘初始化，方便在远程服务器、Docker 等无桌面环境下运行和调试

### ⚡ 优化

- 入库通知详情重试退避改为前密后疏（0.5/1/1.5/2/3/4/5/8s，总等待 43s→25s），刮削几秒完成即可几秒后发通知
- 海报下载超时从 300s 压缩到 8s，失败快速降级纯文本，不再因一张图片卡住整条通知
- Episode 防抖窗口从 15s 缩短到 8s，批量入库时通知更快发出
- 播放停止通知移除「观看时长」字段，播放进度已含时间（如 40:24 / 43:35）不再重复显示

### 🐛 修复

- 修复 Emby 设置页保存时脱敏 API Key（含星号占位符）被写回 settings.json 的问题，真实 Key 不再被覆盖
- 修复 monitor 刷库时 Series 类型入库事件被丢弃的问题，整剧入库现在也能正常发通知

***

## v1.3.0 · 2026-09-05 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.9...v1.3.0)

Emby 通知更可靠、任务历史能删除、默认配置更省心的一版。

### 亮点速览

- Emby 入库通知偶尔收不到的问题修好了，评分、主演等信息也能补齐
- 任务执行历史页面可以正常删除记录、清空全部了
- 首次启动自动生成标准默认配置，清理和路径编码等默认功能都正确开启

### ✨ 新增

- 任务执行历史支持单条删除与清空全部（之前点删除会提示失败）

### ⚡ 优化

- Emby 播放通知对齐参考项目：设备信息更清楚、中文冒号图标统一、观看时长等字段更完整
- Emby 通知海报改为本地化处理，内网 Emby 也能正常显示封面

### 🐛 修复

- 修复 Emby 入库通知有时完全收不到的问题，现在都能正常送达
- 修复入库通知评分、主演等信息取不到、内容不完整的问题（没有演员时不再显示空白行）

***

## v1.2.9 · 2026-09-04 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.8...v1.2.9)

通知内容更丰富、保存设置更顺手、手机端布局更清爽的一版。

### 亮点速览

- Emby 入库/删除通知新增海报、评分、类型、演员、季集时间，剧情简介不再截断
- Telegram 设置保存时误报「机器人令牌无效」的问题已修复，错误提示直接说明原因
- 任务编辑页「删除多余本地文件」等勾选框保存后不再自动取消
- 路径映射和删除同步页手机端改成上下排列，输入框不再被挤出屏幕外

### ✨ 新增

- Emby 通知新增海报、评分、类型、演员、季集时间字段

### ⚡ 优化

- Emby 通知剧情简介不再截断，能看到完整内容
- 没有演员信息的电影不再显示空白行
- 海报图片下载到本地再发送，通知里能正常显示
- Emby 通知标题统一格式，更整齐
- Telegram 设置页标签改成中文，按钮不再挡住输入框
- 路径映射和删除同步页面手机端改成上下排列，不再横向出界
- 账号选择框加宽，「遍历全部账号」显示完整

### 🐛 修复

- 修复 Telegram 设置保存时误报「机器人令牌无效」：明明机器人能用却保存失败，错误提示现在会直接说明是令牌错、需要代理还是网络不通
- 修复任务编辑页勾选框（如「删除多余本地文件」）保存后被自动取消的问题

***

## v1.2.8 · 2026-09-04 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.7...v1.2.8)

界面进一步打磨 + Emby 网页播放修复的一版。

### 亮点速览

- 修复 Emby 反代网页端播放失败，STRM 源一律强制 DirectPlay，不再走转码
- 任务列表表格列宽优化，窄容器下不再横向滚动
- 路径映射 / 删除同步输入框拉长、账号选择列前移，布局更整齐
- Telegram 通知配置更清爽，Bot 状态栏长名字不再截断

### ✨ 新增

- 历史日志详情页单元测试，覆盖 id 匹配、找不到记录、加载失败三个场景

### ⚡ 优化

- 任务列表表格 7 列分配合理宽度，`max-w-5xl` 容器内正常显示，无需横向滚动
- 路径映射（115 网盘 → 本地）与删除同步（Emby → 115 网盘）路径输入框拉长占满空白
- 删除同步账号选择列从最右移到最左，与路径映射布局保持一致
- Telegram 通知两个复选框水平对齐，移除「启动时自动轮询」下方冗余说明
- Telegram Bot 状态栏用户名 / 昵称长文本不再挤压布局
- 任务执行历史标题字号调小调轻
- 精简监控页「全量同步」提示文案

### 🐛 修复

- 修复 Emby 反代网页端播放失败：STRM 源走转码必挂，改为一律强制 DirectPlay，与 Emby App 行为一致

### 🧪 测试

- 新增 log-detail 前端单元测试
- 更新 embyproxy 策略日志与测试断言

***

## v1.2.7 · 2026-09-04 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.6...v1.2.7)

修复设置保存与任务历史日志两个小 bug，页面宽度进一步调优的一版。

### 亮点速览

- 修复「强制代理 UA 标识」清空后保存不生效的问题
- 修复任务历史 → 查看日志显示「历史记录不存在」的 bug
- 全站 PC 端页面宽度统一收敛到 `max-w-5xl`，更紧凑耐看
- 登录页副标题改为「更快、更强、更硬」

### ✨ 新增

- **任务历史日志独立接口**：新增 `GET /api/taskHistory/:executionId/logs`，按执行记录 ID 精确拉取日志

### ⚡ 优化

- 全站 PC 端页面宽度从 `max-w-6xl` 收敛到 `max-w-5xl`（1024px），表单页和列表页都更紧凑
- STRM 清理统计卡片响应式降级：窄容器下少列显示，避免卡片过挤

### 🐛 修复

- 修复「强制代理 UA 标识」清空后保存仍显示旧值的问题
- 修复任务历史 → 查看日志显示「历史记录不存在」且文件数量为 0 的问题
- 修复无 UA 信息时 Emby 反代播放策略偶发判断错误的问题

### 🧪 测试

- 新增任务历史日志接口单元测试
- 新增 UA 标识保存与清空场景单元测试

***

## v1.2.6 · 2026-09-03 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.5...v1.2.6)

STRM 播放路由大改 + 页面宽度统一 + 任务状态更可靠的一版。

### 亮点速览

- Emby 反代：强播放器默认直接播放 STRM，浏览器自动转码兜底，两边不冲突
- STRM 强制代理 UA 标识默认值清空，一般用户无需填写
- 任务详情页去掉飘忽的总进度百分比，改成明确的状态徽章
- 所有页面宽度统一为 6xl，PC 端看着更整齐

### ✨ 新增

- **Emby 反代智能路由**：强播放器走 STRM 直链，浏览器/Web 客户端保留 Emby 转码能力
- **任务详情页状态徽章**：用「开始 / 已完成 / 失败 / 已取消」 + 具体完成数替代不稳定的总进度百分比

### ⚡ 优化

- STRM 路由策略说明改为「默认直连，仅在特殊场景下按需强制代理」，更容易理解
- 启动日志同步新策略，不再显示过时的「强制 DirectPlay 白名单」文案
- STRM 清理「清理失效 + 补生成漏项」按钮改成黑底白字，主次更清楚
- 操作日志面板加宽，长文本不再换行
- 操作日志「清空」按钮改成更轻量的样式，不再撑大标题行
- 所有页面统一最大宽度为 6xl，PC 端视觉更一致

### 🐛 修复

- 修复清理全部失效后统计不准的问题，删完会重新扫描校准
- 修复清理全部失效后失效计数不会立即更新的问题
- 修复 STRM 清理页面组合按钮白底黑字看不清的问题
- 修复操作日志面板列宽太窄导致文字换行的问题


***

## v1.2.5 · 2026-09-02 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.4...v1.2.5)

STRM 清理对账真正能用的一版，顺手修了一堆小细节。

### 亮点速览

- 清理对账 4 个按钮全部修通，不再点了没反应
- 扫描 / 对账两套结果合并成统一视图，多了一张「数据库记录数」统计卡
- 失效 STRM 直接预览内容，不用再点文件管理器翻
- 重复扫描自动走 1 分钟缓存，不再反复请求云端
- 账号状态总览卡片拉宽 + 改成纵向堆叠，信息更不挤
- 新增端到端测试覆盖，后续再改不容易回退

### ✨ 新增

- **STRM 失效预览**：失效列表里点一下文件，弹窗里直接看 STRM 内容，长内容可展开全文
- **扫描缓存**：刚扫完 1 分钟内再扫自动走缓存，工具栏会提示当前用了缓存
- **数据库记录数**：对账明细新增「DB 记录」列，统计表头也多了一张卡
- **账号状态页 UI 优化**：账号状态卡片拉宽，信息改为纵向堆叠，一眼看全更舒服

### ⚡ 优化

- **清理后数据更准**：删完 / 补生完会重新扫一遍本地，显示的 STRM 数量不再越用越偏
- **统计不掺水**：本地 STRM 数量只算 .strm 文件，.nfo、封面图、字幕单独统计，不再混在一起
- **组合按钮提示更准**：「清理失效 + 补生成漏项」确认弹窗按实际选中条目计数，没选中不让点

### 🐛 修复

- 修复「清理全部失效」按钮点了没实际删除
- 修复「补生成全部漏项」按钮没实现、点了没反应
- 修复「清理失效 + 补生成漏项」会删全量失效，而不是只删勾上的
- 修复补生完漏项后，列表没同步刷新的问题
- 修复「全量对账」数据库记录列一直是 0

***

## v1.2.4 · 2026-09-02 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.3...v1.2.4)

Emby 反代热重启版本 —— ProxyPort 和 EmbyURL 修改后即时生效，再也不用重启主程序了。

### ✨ 新增

* Emby 反向代理**热重启**：保存「代理端口」或「Emby 地址」后，反代服务自动优雅切换（旧连接放完、新服务启动），不中断主程序。关闭反代即时停止
* Emby 反代完整单元测试（27 个），覆盖 ISO 原盘 / Audio 类型 / 缓存 TTL / LRU 淘汰 / 并发安全 / 端口热切换 / Stop 超时 / 端口冲突 等全部场景

### ⚡ 优化

* 前端「Emby 反向代理」UI 重设计：checkbox 移到标题左侧对齐，标签整块可点、移动端 hit area 扩大到 36px+
* 移动端布局优化：反代端口下方的 URL 预览**自动换行**（之前长地址会溢出）、「保存配置 / 测试连接」按钮**竖排各占整行**
* Webhook 配置指引底部提示语从灰底卡片改成单行浅色文字，和页面其他说明语风格统一
* 事件类型标签（媒体入库/删除/播放开始等）从灰字灰底改成**黑字白底 + 细边框 + 微阴影**，更醒目
* Docker 用户：docker-compose.yml 自动带上 `8097:8097` 端口映射
* Wiki / Readme 修正轮询间隔描述：**默认 10 秒**（之前多处误写 30 秒，v0.9.7 就从 30 秒缩短到 10 秒了）

### 🐛 修复

* 修复「Emby 反向代理」配置**保存后刷新丢失**：proxyPort 字段之前没进后端白名单，POST 被静默忽略；现在保存后数据库里正常持久化
* 修复「Emby 反向代理」UI **初始化不回显**：checkbox 用 `useState(initialValue)` 只在首次挂载执行一次，此时 settings 还在异步加载，导致永远 false；改成直接从 props 派生
* 修复 `Manager.Start` **端口冲突静默成功**：`ListenAndServe` 在 goroutine 里异步启动，端口被占要等运行时才报错，但 Start 已返回 nil；现在加了同步 `net.Listen` 预检测，冲突立即返回错误
* 修复 `Manager.Stop` **ctx 过期时状态不一致**：ctx 已过期时 Shutdown 会关 listener 但跳过内部状态清理，导致 Status 显示 Running 但实际 HTTP 连不上；现在 ctx 过期直接短路，Shutdown 返回后无论成败都强制清理状态
* 修复 `embyproxy.New()` 对缺 scheme 的 URL（如 `emby.local:8096`）不报错，现在加了 http/https 校验

***

## v1.2.3 · 2026-09-01 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.2...v1.2.3)

统一 STRM 端点、废弃 enable302 开关的架构重构版本，同时修复远程目录浏览和弹窗闪退等一批生产 Bug。

### 🚨 破坏性变更

* `enable302` 配置字段已彻底删除（settings.json / 任务级 / 监控配置），所有 STRM 统一生成 `/api/strm?account=X&pickcode=Y`；旧 STRM 仍兼容（`/api/fs/get` 内部自动转发），建议用 `scripts/rebuild_strm.ps1` / `rebuild_strm.sh` 重建

### ✨ 新增

* 新增 STRM 重建脚本（PowerShell + bash 双版本），一键扫描替换旧 STRM 中的 `/api/fs/get` → `/api/strm`
* 新增 fs 转发一致性单元测试，验证两个端点响应 100% 相同
* 新增监控模块 STRM URL 生成测试，覆盖 prefix 变体 / URL 编码 / pickcode 兜底 5 组用例

### ⚡ 优化

* `/api/fs/get` 和 `/api/strm` 两个端点**逻辑合并**，智能路由（扩展名强制 proxy / UA 匹配 / 并发限流 / 302 预检）统一生效
* **ISO / BDMV / M2TS 原盘**自动走 proxy 模式（支持 Range seek），mkv / mp4 默认走 redirect（零服务器中转）
* 前端删除 enable302 UI 开关及相关类型定义，STRM 路由策略对所有 STRM 始终生效
* golangci-lint 全部通过（errcheck / gosec / unused / goimports 零告警）

### 🐛 修复

* 修复 URL **双重编码**：`http.Redirect` 对已编码的 STRM URL 二次编码导致 `?` 变 `%3F`、`&` 变 `%26`，现改为直接设置 Location 头 + 302 状态码
* 修复 embyproxy mustParseURL **panic 导致服务崩溃**：Emby URL 格式错误时优雅降级，不再硬崩整个服务
* 修复账户状态**假正常**：之前只检查 Cookie 字符串格式，过期也显示「正常」；现新增 `PingCookie` **真实调用 115 API** 验证 Cookie 有效性
* 修复远程目录**二三级错乱**：115 CID/FID 是 19 位整数，超过 JavaScript `Number.MAX_SAFE_INTEGER` 精度被截断，请求的 CID 变了 → 返回根目录内容。修复：后端 CID/FID 改为**字符串输出**，前端类型同步改为 string
* 修复远程目录**只能看一级**：后端无条件输出 `cid: 0`，前端把 0 当有效值请求根目录。修复：仅 `cid > 0` 时才输出；前端无有效 cid 时退用路径请求
* 修复定时任务窗口**闪退**：`nowTs` 每秒变化触发 `useMemo` 重建 columns → 表格重渲染 → Dialog 状态丢失。修复：用 `useRef` 包装时间戳，从依赖中移除

***

## v1.2.2 · 2026-08-31 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.1...v1.2.2)

### 🐛 修复

* 修复 Emby for Kodi Gen 播放 ISO .strm 报「没有兼容的流」：URL `file_name` 和 115 `meta.FileName` 都为空时扩展名白名单短路，302 直跳 CDN 导致 Kodi Lavf HEAD probe 403/无头。`pickOneFileName` 升级 3 档兜底（URL 参数 > 115 meta > CDN 路径 basename PathUnescape），fs/strm 两个 DecideRoute 统一复用。`pickonefilename_test.go` 新增 9 例全覆盖。

* 修复 `/api/fs/get` 漏注册 MethodHead 路由：Lavf ISO probe 第一步严格发 HEAD，原厂只挂了 GET 返回 405，直接拍死整条 probe 链路。补注册 MethodHead 路由 + fs/strm 两处 handler 加 HEAD 短路径（不真转发 CDN HEAD，避免 signed URL 403）。

* 修复 HEAD 响应缺 Content-Length：115 download API 在部分账号/文件下 `meta.FileSize=0`（CDN Range 响应里 `/totalSize` 100% 正确，与 meta 字段解耦）。新增 `totalSizeFromCdn()` 极轻量 `Range: bytes=0-0` GET fallback，从 `/<totalSize>` 解析真实大小写回，成本 <30ms，失败安全回退。

* 修复 HEAD / GET `Access-Control-Expose-Headers` 不一致：跨域发 HEAD probe 时 CORS 吞掉 Content-Length / Accept-Ranges。统一对齐 GET 暴露头列表。

### ⚡ 优化

* 日志升级为单行定位：ISO probe 排错不用拼 3-5 行 grep。新增 4 条关键单行标记：`[FS/HEAD][fallback]` / `[STRM/HEAD][fallback]`（fallback 命中 + 耗时）、`[FS/HEAD]` / `[STRM/HEAD]`（`cl_source=meta` 或 `cdn-range0-0` + size + UA）、`[STRM][proxy-range]`（status + reqRange + respCL + respCR 四字段一行）、`[FS/GET]` method 字段区分 HEAD/GET。排错 grep：`grep -E '\[FS/HEAD\]|\[proxy-range\]|cl_source'`

***

## v1.2.1 · 2026-08-31 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.2.0...v1.2.1)

### ✨ 新增

* 新增账户状态「真实 Ping 检测」：点击「检测状态」按钮后，**真实调用 115 API** 验证 Cookie 是否存活（之前只检查 Cookie 字符串格式，过期也显示「正常」）

### ⚡ 优化

* /api/fs/get（Enable302 模式下的 STRM 路由）也加入**智能路由决策**：ISO / BDMV / M2TS / VOB / TS 等需要 Range seek 的格式自动强制 proxy 模式，不再写死 302 redirect

### 🐛 修复

* 修复 Emby Proxy 在 Emby URL 配置错误时 mustParseURL **直接 panic** 导致整个服务崩溃的问题（改为返回 error 优雅降级，主服务不受影响）

* 修复任务历史页面**白屏**问题：前端 history.tsx 的 JSON 字段与后端 Go struct 返回的平铺结构不匹配，导致 TypeError 整个页面渲染中断

* 修复 /api/fs/get 路由写死 302 redirect 导致 ISO 原盘在 Kodi / CoreELEC 中 Libdvd 无法 Range seek 的问题（block 256 / 384 / 32760 全部 seek 失败）

* golangci-lint 4 项修复（errcheck / gosec / unused / goimports）

***

## v1.2.0 · 2026-08-31 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.9...v1.2.0)

### 🐛 修复

* 修复任务执行历史页面显示空白：executor 未调用 TaskHistoryRepo 写入数据库，导致历史记录永远为空

***

## v1.1.9 · 2026-08-31 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.8...v1.1.9)

### ✨ 新增

* 新增 STRM 智能路由「扩展名强制 proxy」规则：.iso / .bdmv / .bdav / .ts / .m2ts / .vob / .ifo / .bup 自动识别为 seek_required 格式，无论 UA 匹配与否都走 proxy 模式，保证 Kodi / Libdvd 能正常 Range seek

***

## v1.1.8 · 2026-08-31 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.7...v1.1.8)

### ✨ 新增

* 新增 Emby 反向代理服务（embyproxy）：拦截 Emby PlaybackInfo API 请求，识别 STRM 源文件（IsRemote=true + Protocol=Http 或 Path 以 http:// 开头）

* PlaybackInfo 拦截自动**强制 DirectPlay**：改写 SupportsDirectPlay=true、移除 TranscodingUrl、缓存 STRM MediaSourceId → HTTP URL 映射

* HandleMediaStream：媒体流请求优先返回 STRM 缓存的 302，否则透传到 Emby

* CI go test 加 -timeout=15m -race 防超时和竞态

***

***

## v1.1.7 · 2026-08-31 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.6...v1.1.7)

Emby/Kodi STRM 播放修复 + CI 稳定性加固。EmbyServer 加入强制代理 UA 默认列表，解决 CDN UA 绑定导致的 ISO 原盘播放失败。

### ✨ 新增

* EmbyServer 加入强制代理 UA 默认列表，Emby 服务器请求自动走代理模式
* CDN 直链缓存清理 API（`POST /api/strm/cache/clear?ua=xxx`）
* UA 诊断日志，STRM/FS 请求行输出请求方 UA
* pkg/strmutil + pkg/logger 测试补全（覆盖率 93.8% / 100%）

### ⚡ 优化

* Telegram webhook secret 校验提前到 token 检查之前
* 测试隔离避免 CI 偶发超时（空 BotToken 跳过 getMe API）

### 🐛 修复

* CI goimports 格式修复（import 三段分组）
* gosec G306 测试文件权限修复（0644→0600）

***

## v1.1.6 · 2026-08-29 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.5...v1.1.6)

日常使用回归版本：Emby 通知完整、rename 干净、STRM 路由更稳、账号异常不再卡住。

### 🐛 修复

* **Emby 入库通知评分/主演/海报缺失** — 入库后多等一会儿让 Emby 刮削完再发通知（之前被父进程超时掐断了），同时主动请求完整元数据
* **rename/move 后旧目录残留** — 根文件夹路径没写入数据库、级联删除只删文件不删文件夹表，两处补齐
* **rename/move 信任了过期缓存** — 这类事件到达时数据库存的还是旧路径，现在跳过缓存强制走 API 刷新
* **创建 STRM 时数据库写入时机不对** — "已存在则跳过"模式下不生成 STRM 导致数据库也没记录，后续 move/rename 反查不到旧路径
* **.iso 电影重命名时关联资源丢失** — 去掉了 .iso 特殊处理，和其他文件一样只去掉最后一个扩展名，重命名 .nfo/.jpg/.srt 等不再漏掉
* **STRM 路由缓存串号** — 不同浏览器/设备的 User-Agent 可能拿到不同的 115 CDN 地址，现在缓存 key 带上 UA，避免换个设备就打不开

### ✨ 优化

* **账号异常不再卡住 STRM 生成** — 之前连续几次轮询失败后账号会退避冷却（从 2 分钟到 30 分钟），冷却期内该账号所有 STRM 都不生成。现在退避机制禁用，保持配置的轮询间隔
* **监控状态页信息补全** — Web 面板账号状态新增游标位置（FromID/FromTime）、连续错误数、退避状态等字段，方便排查
* **设置页监控配置读写修正**

### ✨ 其他

* 新增单元测试覆盖退避禁用、cookie 失效通知去重
* 移除重复的 wiki-sync workflow，保留带 Release 自动同步的那一个

***

## v1.1.5 · 2026-08-29 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.4...v1.1.5)

通知体验优化版本：消除账号异常与批量删除时的 TG 刷屏。

### ⚡ 优化

* 删除通知合并 — Emby 同步删除和 115 监控删除统一成一条通知，整剧删光只发一条，不再每集刷屏
* 115 账号出错自动降频 — cookie 失效后冷却 30 分钟，连续失败阶梯退避，避免撞风控和 TG 刷屏
* Emby 剧集入库通知等待刮削 — 入库后会多等一会儿让 Emby 刮削完再发通知，不再出现"暂无简介/主演"

***

## v1.1.4 · 2026-08-28 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.3...v1.1.4)

开源工程化版本：补齐协作文档，加固签名容错，清理仓库。

### ✨ 新增

* 贡献指南、安全策略文档上线
* STRM URL 签名 secret 自动生成功能的单元测试

### 🐛 修复

* 配置文件损坏或被截断时 STRM 签名 secret 自动重新生成，保证签名始终可用
* 清理误入仓库的本地调试脚本

***

## v1.1.3 · 2026-08-28 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.2...v1.1.3)

质量加固版本：大幅补充单元测试，静态检查全面通过。

### ✨ 新增

* 约 180 个单元测试用例，覆盖账号、通知、STRM 路由、Emby、任务等核心模块

### ⚡ 优化

* 启用全部代码静态检查规则并修复所有历史告警

***

## v1.1.2 · 2026-08-28 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.1...v1.1.2)

首个 CI 驱动的工程化版本：Go Test + 静态检查 + 跨平台构建 + Swagger API 文档 + E2E 冒烟测试。

### ✨ 新增

* GitHub Actions CI — Go 测试、静态检查、前端构建、跨平台原生二进制（Linux amd64/arm64、Windows amd64）一键流水线
* Swagger UI — 在线浏览 API 定义、填参数直接调试
* Playwright E2E 冒烟测试 — 4 条覆盖健康检查、Swagger、登录页、错误反馈

### 🐛 修复

* 老用户 settings.json 迁移补齐 ISO 扩展名
* 清理无效的静态检查配置项

***

## v1.1.1 · 2026-08-28 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.1.0...v1.1.1)

STRM 签名 + 通知模板完善版本。

### ✨ 新增

* STRM URL 自动追加 HMAC-SHA256 签名 token，防止链接被盗用
* Telegram 电影/剧集入库通知结构化模板 — 带评分、类型、主演、入库时间、封面图
* Emby library.new webhook 端点，入库即触发通知

### ⚡ 优化

* SQLite Schema 迁移框架，后续加字段不用手动改表
* 数据库连接池配置化

***

## v1.1.0 · 2026-08-26 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.9...v1.1.0)

首个 Go 重构正式版：跨平台原生二进制、Web 管理面板、飞牛 fNOS 打包。

### ✨ 新增

* Go 语言全量重写 — 单文件原生二进制，无 Python 运行时依赖
* Web 管理面板（Vite + React）— 账号管理、监控状态、任务列表、系统设置全部可视化
* 托盘菜单 — Windows 系统托盘显示运行状态、打开 Web 界面、退出
* 飞牛 fNOS 打包 — 官方 fnpack 构建 fpk 安装包，支持 amd64/arm64
* Docker 多架构镜像 — linux/amd64 + linux/arm64 一键部署
* API Key 认证 + JWT 登录 — 保护管理接口不被未授权访问

### ⚡ 优化

* Windows 默认访问地址显示为 localhost:8090（原来 0.0.0.0 无法直接打开）
* 监控状态按钮按账号真实情况分化 — 运行/停止/异常/不存在各显示对应操作
* 新增"从监控列表移除"功能，一键清理无效监控配置

### 🐛 修复

* 编辑账号同时改名称和 Cookie 时，Cookie 没保存
* 监控引用已删除账号时按钮还显示"停止"，改为"移除"
* 任务列表为空时前端白屏

***

## v1.0.9 · 2026-08-25 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.8...v1.0.9)

> 📌 本版本已包含 v1.0.8 全部内容

Windows 系统托盘、移动端 UI 全面优化、Emby 通知配置热更新、默认路由调整。

### ✨ 新增

#### Windows 系统托盘

* 新增 Windows 系统托盘图标支持：使用 `github.com/getlantern/systray` 实现，exe 启动后自动在右下角托盘显示 FastStrm 图标

* 托盘菜单：打开 Web 界面、查看运行状态、服务器连接状态、关于、退出

* 实时状态更新：每 2 秒检测服务器健康状态并更新托盘提示

* 优雅关闭：通过托盘菜单退出时正确关闭 HTTP 服务

#### 账号安全独立页面

* 新增「账号安全」独立页面（`/account-security`），从设置页拆分为独立侧边栏菜单项

* 账号安全与设置风格一致，包含密码修改、会话管理等功能

### 🔥 删除

#### Emby 通知页面

* 删除「注意」提示区块，简化页面

### ⚡ 优化

#### 移动端 UI 全面优化

* Tab 栏：添加 `shrink-0` 和 `whitespace-nowrap` 防止移动端换行

* 卡片内边距：从 `p-4 sm:p-5` 调整为 `p-3 sm:p-5`，移动端更紧凑

* 保存按钮：固定底部位置（`sticky bottom-0`），方便操作

* 响应式按钮：操作按钮添加 `w-full sm:w-auto`，移动端全宽

* 路径映射：移动端改为垂直排列（`flex-col sm:flex-row`）

* 任务管理页面：全新移动端卡片布局，与账户管理页面风格一致

* STRM 清理页面：统计改为 `grid grid-cols-2 sm:grid-cols-4` 响应式网格

#### 默认路由

* 首页默认路由从 `/task` 改为 `/account`，首次访问进入账户管理页面

* 侧边栏 Logo 链接指向 `/account`

#### 前端缓存

* `index.html` 改为永不缓存策略（`no-cache, no-store, must-revalidate`），确保版本更新后浏览器总能获取最新 SPA 入口

* JS/CSS 资源保持 24 小时缓存（文件名含内容哈希，安全）

#### Emby 通知系统

* Notifier 改为动态创建 Client：移除启动时缓存的 Client 实例，每次从当前配置创建，用户修改 Emby URL/APIKey 后立即生效

* 用户 ID 跨 Client 缓存：在 Notifier 级别缓存 Emby 用户 ID，避免每次通知重复请求 `/emby/Users`

* `SyncDelete`、`MediaServerRefresh` 均改为动态 Client 创建

#### Windows 打包

* 图标文件 `favicon.ico` 嵌入 exe 作为应用图标

* 构建配置中添加 `rsrc` 工具调用，自动生成 Windows 资源文件

### 🐛 修复

* 修复 Emby 通知使用缓存 Client 导致用户修改配置后通知仍使用旧配置的问题

* 修复用户 ID 在每次创建新 Client 时丢失、重复请求 Emby 用户列表的问题

* 修复首页默认路由跳转到 `/task` 而非 `/account` 的问题

* 修复 `index.html` 被浏览器缓存 24 小时导致版本更新后前端不生效的问题

* 修复 Windows 版本缺少系统托盘图标和菜单的问题

* 修复 `systray.ShowMessage` 函数不存在导致编译失败的问题（改为日志输出 + 托盘提示）

***

## v1.0.8 · 2026-08-25 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.7...v1.0.8)

> 📌 本版本已包含 v1.0.7 全部内容

Emby API 认证修复、详情获取三级回退策略、TG Bot 命令补齐。

### ⚡ 优化

#### Emby API 客户端

* 移除所有 API 请求中对 `API Key` 和 `ID` 的 `url.QueryEscape` / `url.PathEscape` 编码，直接拼接字符串，与 qmediasync 保持一致

* `GetItemDetail` 重构为三级回退策略：①优先使用缓存的有权限用户 → ②尝试所有可用用户（新增 `GetAllUsers` 方法）→ ③无用户上下文降级

* `GetItemDetailWithRetry` 重试次数从 4 增至 5，移除仅 404 重试限制（所有错误均触发重试），增加详细日志

#### Telegram Bot

* `BotCommandList` 添加 `/start` 命令

### 🐛 修复

* 修复 Emby API 因 URL 编码导致认证失败的问题（`API Key` 中的特殊字符被转义后无法通过认证）

* 修复 Emby 实例无 `EnableAllFolders=true` 用户时入库通知详情获取失败的问题

* 修复 TG Bot 缺少 `/start` 命令的问题

***

## v1.0.7 · 2026-08-25 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.6...v1.0.7)

> 📌 本版本已包含 v1.0.6 全部内容

Telegram 通知代理支持、重试机制全面上线，生活事件监控可靠性增强，通知排版与 Emby 集成优化。

### ⚡ 优化

#### Telegram 通知

* Metadata 展示顺序改为业务优先级（账号 / 类型在前 → 路径 → 度量 → 简介 → 时间在后），不再按 UTF-8 字节序排序，重点信息一眼可见

* 批量合并通知「👤 账号:」半角冒号改为全角「👤 账号：」，与 `FormatMessage` 统一渲染层风格一致

* `/scan` `/cleanup` 命令直接打开 STRM 操作菜单（全量同步 / 清理孤儿），不再显示占位文本

* `/help` 帮助文本全面重写，按实际可用命令 / 主菜单结构 / 任务执行 / 监控启停分组说明

* STRM 创建 / 删除 / 移动 / 重命名通知改用统一的 `FormatMessage` 三段式渲染，字段顺序与样式保持稳定

* 任务完成 / 任务错误通知改用 `FormatMessage` 统一渲染，与其他通知风格一致

#### Emby 通知

* 播放停止 emoji 由禁止符号 `⛔` 改为标准停止符号 `⏹️`，与参考项目一致

* 播放通知 metadata 字段「进度」改为「播放进度」，语义更清晰

* 播放通知配图策略由 Backdrop（横版背景）优先改为只取 Primary（竖版海报），与播放进度文字搭配视觉更协调

* 入库 / 删除 / 播放通知模板全面改用统一的 `FormatMessage` 三段式渲染层（Title / Content / Metadata）

#### 通用

* 新增 `BuildPrimaryImageURL` 方法，专门用于需要竖版海报的场景（播放通知）；入库通知继续走 Backdrop 优先的 `BuildImageURLIfAvailable`

***

## v1.0.5 · 2026-08-24 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.4...v1.0.5)

> 📌 本版本已包含 v1.0.4 全部修复内容

生活事件监控可靠性增强：删除事件增加熔断保护、新建文件夹处理、关联文件删除安全校验等。

### ✨ 新增

* 新增删除事件熔断保护：单次轮询删除事件超过 100 条或占比超过 50% 时，自动跳过本次所有删除操作，防止 API 异常返回大量删除事件导致大规模误删

* 新增「仅错误通知」开关：LifeMonitor 设置页开启后，正常的创建 / 删除 / 移动 / 重命名不推 Telegram，仅异常时推送

### 🔥 删除

* 彻底移除「硬删除」配置与 UI 开关：所有删除操作统一为物理删除（直接删除），不再生成 `.deleted.bak` 备份文件，统一行为避免混淆

### ⚡ 优化

* Emby webhook URL 复制支持 HTTP 环境降级处理

* 前后端硬删除配置统一，UI 与后端配置项一致

### 🐛 修复

* 修复新建空文件夹事件被误判为媒体文件、错误生成 STRM 的问题

* 修复关联文件删除可能误删非关联文件的问题（例如之前 `movie` 可能误删 `movie_trailer.mp4`，现已加扩展名与 remainder 双重校验）

***

## v1.0.4 · 2026-08-24 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.3...v1.0.4)

发布后很快被 v1.0.5 覆盖，仅包含两个紧急修复。

### 🐛 修复

* 修复移动 / 重命名事件目标路径已存在时被直接跳过、STRM 不更新的问题，现改为 fallback 到 recreate 模式（清理旧 STRM + 重新生成）

* 修复 Emby 入库通知详情获取偶发 404 的问题，新增自动重试逻辑应对 Emby 异步写入延迟

***

## v1.0.3 · 2026-08-24 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.2...v1.0.3)

### 🐛 修复

* 修复 115 生活事件 API 响应字段类型变化导致事件拉取直接失败的问题（count 字段变成字符串、next\_page 变成非 bool 类型），现已兼容所有类型

***

## v1.0.2 · 2026-08-24 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.1...v1.0.2)

修复生活事件监控生成 STRM 完全不生效的致命问题，并新增「错误主动推送 + 仅错误模式」，运行更安心。

### ✨ 新增

* 新增监控错误主动推送 Telegram：Cookie 失效 / 轮询异常 / 批量事件处理失败，出错必达，不再"静默失败"

* 新增「仅错误时发 TG 通知」开关：开启后正常的创建 / 删除 / 移动 / 重命名不刷屏，只在异常时推送

* 新增 Emby webhook 入口诊断日志：入库通知不生效时可以看日志确认 webhook 是否真的到达

### ⚡ 优化

* 媒体路径查询独立固定走官方稳定通道，不再被生活事件 API 的备用线路切换影响，避免回归

### 🐛 修复

* 修复生活事件监控生成 STRM 完全不生效的致命问题（旧接口返回事件缺少 pick\_code，导致所有创建事件被跳过），现改为优先走字段完整的 iOS 端接口，失败时回退 web 接口

* 修复 STRM 删除时默认生成 `.deleted.bak` 备份占磁盘的问题：默认改为硬删除，并提供配置开关；旧配置自动迁移

* 修复对账清理 auto\_clean 模式仍生成 .bak 残留的问题

* 修复 Emby webhook URL 在 HTTP 环境下点击复制报错的问题（降级为兼容方式）

***

## v1.0.1 · 2026-08-24 · [对比](https://github.com/wabisabi926/faststrm/compare/v1.0.0...v1.0.1)

大库首次生成时「目录扫描看起来卡死、0% 一直不动」的感知焦虑彻底解决。同时对齐参考项目 qmediasync 的 Telegram 与 Emby 通知模板，通知美观度与信息完整度大幅提升。

### ✨ 新增

* 新增任务面板 9 阶段徽章：🔍 扫描目录 / 📊 增量比对 / 🧹 清理文件 / 💾 写数据库 / ⚡ 生成 STRM / 🔧 收尾等，一眼知道在做什么

* 新增任务「已用时间」计时器（每秒刷新），大库运行时一眼判断任务"还活着"

* 新增扫描阶段骨架动画进度条 + 每 3 秒「已扫描 N 目录 / M 文件 / 命中 K 个媒体」心跳广播，目录多的时候 UI 不再静止 0%

### ⚡ 优化

* Emby 通知模板对齐参考项目：清晰度 / 设备信息 / 播放格式 / 封面图尺寸全部升级

* Emby 入库 / 播放 / 暂停 / 停止四类通知排版对齐 qmediasync 风格

* 图片发送链路统一：下载临时文件再上传，彻底解决内网 Emby 无法被 TG 服务器访问导致无封面的问题

* 任务执行所有 9 个阶段均实时同步到前端，失败时同步错误原文

### 🐛 修复

* 修复 Emby 播放 / 入库等通知完全不发送的问题

* 修复 STRM 任务启动 / 完成 / 失败时 Telegram 通知不触发的问题

* 修复 Telegram Bot 按钮点击无响应的问题

* 修复开机自动轮询不启动的问题

* 修复 Telegram 通知中文与 Emoji 排版错位的问题

* 修复生活监控通知重复发送的问题

* 修复 Emby 通知缺少封面图的问题（大小写不匹配 + 内网不可达）

* 修复 Emby 播放暂停 / 停止通知缺少「⌛ 观看时长」的问题

***

## v1.0.0 · 2026-08-22 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.9...v1.0.0)

生活事件监控完全对齐参考项目 p115strmhelper，STRM 生成 / 移动 / 重命名 / 删除的核心逻辑全面重写，稳定性与准确性大幅提升。

### ✨ 新增

* 新增四象限决策矩阵（普通目录↔媒体目录之间的创建 / 移动 / 删除 / 跳过），事件处理更精准

* 新增事件类型严格路由：按 115 事件类型分别走创建 / 移动 / 重命名 / 删除四条专属处理分支

* 新增 STRM 清理双阈值二次确认：超过 10 条直接拒绝、5–10 条需 UI 确认，防误删

* 新增 STRM 操作历史记录 API：可查看每次全量 / 增量 / 监控 / 重命名 / 移动的执行结果、耗时、错误详情

* 新增 60 秒通知合并器：同一账号同类型事件 60 秒内自动合并，避免通知刷屏

* 新增 STRM 生成黑白名单过滤（如自动跳过 `-trailer`、`sample`、`.cd2` 等非正片文件）

* 新增 STRM 删除时可选同步清理关联文件（.nfo / .jpg / .srt / .ass / .sub）

* 新增飞牛 fNOS 安装向导端口自定义配置

### ⚡ 优化

* 115 API：多 User-Agent 轮换池 + 429 自动切换 + 域名修正 + 分级超时（普通 30s / 慢操作 300s），更不容易被限流

* 302 直连路由：405 自动降级 + 二次 pickcode 检索 + 并发锁，播放成功率更高

* Emby 自动刷库：新增 10 秒防抖，避免频繁触发媒体库扫描

* SQLite：WAL 模式 + busy\_timeout + 单连接，并发读写更稳定

* 数据库孤儿清理：分页扫描 + STRM 清理后立即触发，DB 与本地文件状态一致

### 🐛 修复

* 修复 STRM 路径映射丢失子目录的问题（4 处误用，STRM 会生成到父目录而不是映射目录）

* 修复 DB 提前覆盖旧路径，导致旧 STRM 被误判为「不存在」而无法正确删除的问题

* 修复 recreate 模式下受配置影响不生成新 STRM 的问题

* 修复四象限 OTHER\_TO\_MEDIA 场景旧 STRM 未清理、造成同名重复文件的问题

* 修复 rename/move fallback 逻辑导致新旧 STRM 并存的问题（失败直接返回错误，行为与参考项目一致）

***

## v0.9.9 · 2026-08-21 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.8...v0.9.9)

### ✨ 新增

* 新增 Emby 媒体库刷库配置 UI：可分别设置创建/删除后是否自动刷库、以及防抖间隔秒数

* 新增本地目录子项列表接口，前端目录选择器功能完整可用

* 新增账户 Cookie 检查字段：上次检查时间 + 当前是否有效

### ⚡ 优化

* 前后端 Task / Account 类型定义统一，接口契约更清晰

* 账户页面状态管理重构，Cookie 检查与页面展示时序更合理

* 清理调用不存在接口的孤立组件，代码库更整洁

### 🐛 修复

* 修复前后端路由契约冲突（前端调不存在的接口）

* 修复前后端 Task 状态值不匹配（运行中/成功/取消三个状态对齐）

* 修复任务启动 / 取消请求字段名不一致导致操作失败的问题

* 修复 Telegram 通知 data 为空时解析失败的问题

***

## v0.9.8 · 2026-08-21 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.7...v0.9.8)

### ✨ 新增

* 新增 Emby 通知默认勾选选项，上手更省心

* 新增账户「真实连接测试」Deep Check，Cookie 是否有效更精准

* 新增 STRM 清理缓存 1 小时自动过期，防止清理结果与实际不一致

* 新增 Webhook 未配置时的 Debug 日志提示，方便排错

### ⚡ 优化

* Emby 路径选择器新增 Tooltip 提示，操作更友好

* STRM 前缀输入框 placeholder 说明更清晰，减少配置误解

### 🐛 修复

* 修复生活事件监控 SSL 证书验证失败的问题（修正域名为 webapi.115.com）

* 修复事件处理字段名不匹配导致部分事件无法处理的问题

* 修复任务「开始」按钮点了没反应的问题

* 修复 STRM 清理页面统计显示 undefined 的问题

* 修复 Telegram 通知 info 类型报 invalid data 错误

* 修复账户页面敏感字段未脱敏、页面加载时序错乱的问题

***

## v0.9.7 · 2026-08-21 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.6...v0.9.7)

### ✨ 新增

* 新增事件路径自动解析，接口不返回路径时也能正确处理文件事件

* 新增 API 域名自动切换，遇到网络异常自动走备用线路，不用手动改配置

* 新增事件过滤：纯浏览、标星等非文件变更操作不再触发通知

### ⚡ 优化

* 事件轮询间隔由 30 秒缩短到 10 秒，网盘变更同步更及时

* 整体请求失败率降低，监控运行更稳定

### 🐛 修复

* 修复生活事件监控无法连接 115 服务器的问题（SSL 证书错误）

* 修复部分文件事件无法解析路径、导致监控中断的问题

* 修复扫码登录偶发失败的问题

***

## v0.9.6 · 2026-08-21 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.5...v0.9.6)

飞牛 fNOS 路径权限与 qmediasync 完全对齐，修复应用目录选择、任务日志查看、监控状态显示等关键场景。

### ✨ 新增

* 新增 fNOS 安装向导自定义端口功能（默认 8090），端口号持久化在用户数据区，升级不丢失

* 新增本地目录选择器「空列表友好提示」，未授权时明确引导用户在飞牛应用设置中勾选「存储卷」权限

### 🔥 删除

* 移除「选择远程路径后自动填写本地路径」的错误弹窗（之前会把本地路径填成无效的同名目录）

* 精简 fNOS 应用图标尺寸，仅保留官方规范要求的 64×64 和 256×256 两种

### ⚡ 优化

* fNOS 目录权限与 qmediasync 行为一致：只返回用户实际授权的路径，不再列出 20+ 无关的系统挂载点

* 监控状态显示优先级：有错误 → 红色异常；运行中 → 绿色；待保存 → 黄色；已停止 → 灰色，之前"运行中但有错误仍显示绿色"的误导已修复

* fNOS 默认管理员密码统一为 `admin`（与参考项目一致）

### 🐛 修复

* 修复任务日志查看完全失效的问题（前后端契约不匹配）

* 修复生活事件监控 API 返回 404 导致无法拉取事件的问题（修正域名 + 路径 + 请求方式）

* 修复 fNOS 安装向导泄露默认密码的问题

* 修复监控停止、系统关闭时被误记为错误的问题

***

## v0.9.5 · 2026-08-20 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.4...v0.9.5)

飞牛 fNOS / Docker / Linux / Windows 跨平台目录浏览体验大修。

### ✨ 新增

* 新增本地目录选择器顶部「手动路径输入」+「跳转」按钮，目录树看不到时也能手动粘贴路径定位

* 新增三路状态反馈：✅ 成功 / ❌ 错误（显示后端原因）/ 💡 输入提示，用户一眼知道当前状态

* 新增环境变量 `FASTSTRM_LOCAL_DIR_ROOTS`，可显式覆盖本地根目录列表

### ⚡ 优化

* 本地根目录识别改为 4 级优先策略（显式覆盖 → 真实挂载点 → fNOS 共享卷 → 兜底），飞牛、Docker、Linux、Windows 下都能看到正确的可用目录

* fNOS 默认目录权限由「严白名单」改为「宽松放行 + warning 日志」，用户已授权的目录不会再被 403 拒绝

* CI 流程升级到 Node 24 native，消除构建告警

### 🐛 修复

* 修复飞牛 fNOS 沙箱下只能看到一个目录、无法选择媒体文件夹的 P0 问题

* 修复 fNOS 共享目录未声明导致沙箱里真正访问不到的问题（manifest 已补齐 share\_dirs 规范声明）

***

## v0.9.4 · 2026-08-20 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.3...v0.9.4)

### ✨ 新增

* 账户页面新增账户状态图标（正常 / 异常 / 未知），并支持一键刷新检测

### ⚡ 优化

* 远程目录浏览改为优先使用 CID 直接导航，跳转更快更稳

* fNOS 下目录权限识别扩展，新增 TRIM\_DATA\_\* 环境变量支持

* Windows 下本地盘符自动枚举，无需手动输入路径

### 🐛 修复

* 修复远程目录浏览偶发空白、文件夹判断错误的问题

* 修复账户状态无法正确显示的问题

* 修复路径格式处理异常导致目录列表错乱的问题

***

## v0.9.3 · 2026-08-20 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.2...v0.9.3)

### ⚡ 优化

* 系统整体容错能力提升：设置读取失败、任务列表失败、媒体挂载读取失败时自动降级，不再直接 500 报错退出

* 本地目录根路径识别更智能，优先使用 fNOS 环境变量和 /mnt、/media、/home 等常见挂载点

### 🐛 修复

* 修复飞牛 fNOS 下应用无法启动的问题

* 修复目录浏览接口偶发返回错误 id 的问题

* 修复本地目录浏览存在的路径穿越风险

***

## v0.9.2 · 2026-08-20 · [对比](https://github.com/wabisabi926/faststrm/compare/v0.9.1...v0.9.2)

### ✨ 新增

* 新增 STRM 写入并发数可配置，可根据服务器性能调整

* 新增自动下载元数据（nfo/jpg/png 等）开关

* 新增生活事件监控去重配置，避免重复处理

* 新增监控 API 冷却与重试配置，提高稳定性

### ⚡ 优化

* 任务执行时的资源释放更彻底，避免内存泄漏

* 读取配置、序列化等出错时自动回退默认值，不再直接中断

### 🐛 修复

* 修复任务执行过程中 cancel 函数未调用导致的资源泄漏

* 修复多处错误被静默忽略（读取设置、JSON 编解码、目录遍历、Emby 请求等）

* 修复本地目录浏览存在路径穿越的安全风险

***

## v0.9.1 · 2026-08-19 · [首个 Go 重构版本](https://github.com/wabisabi926/faststrm/tag/v0.9.1)

### ✨ 新增

* 完成 Go 语言重构，彻底移除 Node.js 和 Nginx 依赖，单二进制即可部署

* 实现 115 扫码登录，手机扫一扫即可获取 Cookie，过期再扫一下就好

* 实现生活事件监控，网盘增删改自动同步到本地

* 实现 STRM 文件自动生成与清理对账

* 实现 Emby 主动刷库联动 + 删除同步

* 实现 Telegram / Webhook 消息通知

* 实现 302 直连播放模式，不占用服务器带宽

### ⚡ 优化

* UI 统一为简洁风格

* 前端升级为 Vite + React 技术栈

* 后端升级为 Go 单二进制架构，部署更简单


